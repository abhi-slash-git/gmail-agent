# AWS Bedrock Configuration (Required)
# You need AWS credentials with access to Amazon Bedrock and Claude models
AMAZON_BEDROCK_REGION=us-east-1
AMAZON_BEDROCK_ACCESS_KEY_ID=your_aws_access_key_id
AMAZON_BEDROCK_SECRET_ACCESS_KEY=your_aws_secret_access_key

# Google OAuth Credentials (Required for Gmail access)
# Get these from https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Database Configuration (Optional)
# Defaults to ~/.gmail-agent/data if not specified
# DATABASE_URL=/path/to/your/database

# User ID (Optional)
# Defaults to "default_user" if not specified
# USER_ID=your_user_id